# Core
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
numpy>=1.26
httpx>=0.27

# Binary document extraction (SharePoint/Teams connector)
# .docx is parsed with the stdlib (zipfile + ElementTree) — no lxml needed.
pypdf>=4.0

# Optional (prod) — install as needed
# psycopg2-binary>=2.9      # Postgres driver
# pgvector>=0.2             # pgvector SQLAlchemy support
# anthropic>=0.40           # Anthropic provider
# openai>=1.30              # OpenAI provider

# Dev
pytest>=8.0
